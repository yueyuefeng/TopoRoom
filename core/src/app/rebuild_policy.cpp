#include "toporoom/app/rebuild_policy.hpp"

// Rebuild policy lives in document_io.cpp next to commit().
namespace toporoom::app {
int rebuild_policy_module() { return 0; }
}  // namespace toporoom::app
