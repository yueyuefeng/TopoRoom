# Legacy InteractionShell (quarantined)

These scenes are **not** on the exported run path. `res://app/main.tscn` boots
`res://app/joyplan_flow/` only.

The old workbench (photo_stub docks, FAB, three-zone chrome, roam HUD) stays
here so C++ / Session / lighting / PlanCanvas history is not lost. Do not
`change_scene_to_file` these from product screens.
